name: ShellCommands

on: [push]
on
jobs:
  run-shell-commands:
    run-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello"
      - name: multiline script
        run: |
          node - v
          npm -v
